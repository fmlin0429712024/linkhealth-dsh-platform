# dsh-plugins/cdi-gui — placeholder (Step 2)

**Not started yet.** This directory is reserved for the CDI plugin's
**client half**: a browser bundle (exporting `./client`) that registers the
customized domain UI into the DSH shell's slots — audit-finding cards in
`conversation`, evidence/rule detail in `details`, a human-review workbench
in `shell.overlay`, and CDI settings in `settings.plugins.tab`.

Step 1 (`../cdi`) must be running first. See `../cdi/README.md`.
